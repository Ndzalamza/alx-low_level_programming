#include "main.h"

/**
 * * main - tests function that prints if integer is positive or negative
 * * Return: 0
 * */

int main(void)
{
	^I^I^Iint i;

		^I^Ii = 98;
			^Ipositive_or_negative(i);

		return (0);
}
